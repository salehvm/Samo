Pod::Spec.new do |s|

  s.name         = "Samo"
  s.version      = "0.0.1"
  s.summary      = "Samo soon..."
  s.description  = "Samo soon..."
  s.homepage     = "https://github.com/salehvm/Samo.git"
  s.license      = "MIT"
  s.author       = { "saleh" => "infosalehmajidov@gmail.com" }
  s.platform     = :ios, "10.0"
  s.source       = { :git => "https://github.com/salehvm/Samo.git", :tag => "0.0.1" }
  s.source_files = "Samo/**/*"

end
